apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-inventory
  namespace: mybookstore
  labels:
    app: frontend-inventory
    tier: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-inventory
  template:
    metadata:
      labels:
        app: frontend-inventory
        tier: frontend
    spec:
      containers:
      - name: frontend-inventory
        image: mybookstore-inventory-frontend:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8082
          name: http
        envFrom:
        - configMapRef:
            name: mybookstore-config
        livenessProbe:
          httpGet:
            path: /
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "25m"
          limits:
            memory: "128Mi"
            cpu: "100m"