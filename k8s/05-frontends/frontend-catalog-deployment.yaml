apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-catalog
  namespace: mybookstore
  labels:
    app: frontend-catalog
    tier: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-catalog
  template:
    metadata:
      labels:
        app: frontend-catalog
        tier: frontend
    spec:
      containers:
      - name: frontend-catalog
        image: mybookstore-catalog-frontend:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8081
          name: http
        envFrom:
        - configMapRef:
            name: mybookstore-config
        livenessProbe:
          httpGet:
            path: /
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "25m"
          limits:
            memory: "128Mi"
            cpu: "100m"