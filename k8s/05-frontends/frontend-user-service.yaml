apiVersion: v1
kind: Service
metadata:
  name: frontend-user
  namespace: mybookstore
  labels:
    app: frontend-user
    tier: frontend
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    name: http
  selector:
    app: frontend-user