apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-cart
  namespace: mybookstore
  labels:
    app: frontend-cart
    tier: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-cart
  template:
    metadata:
      labels:
        app: frontend-cart
        tier: frontend
    spec:
      containers:
      - name: frontend-cart
        image: mybookstore-cart-frontend:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8085
          name: http
        envFrom:
        - configMapRef:
            name: mybookstore-config
        livenessProbe:
          httpGet:
            path: /
            port: 8085
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8085
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "25m"
          limits:
            memory: "128Mi"
            cpu: "100m"